export type user = {
    id: string,
    name: string,
    email: string,
    password: string,
    role: string
};

export type userInput = {
    name: string,
    email: string,
    password: string,
    role: string
};